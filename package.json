{
  "devDependencies": {
    "firebase": "^7.13.1",
    "firebase-tools": "^8.9.0"
  },
  "scripts": {
    "clean": "rm -rf ./node_modules",
    "reinstall": "npm run clean && npm install",
    "rebuild": "npm run clean && npm install && npm run build",
    "killLocal": "if lsof -t -i:5000; then kill $(lsof -t -i:5000); fi",
    "local": "npm run killLocal && firebase serve -P dev --only hosting"
  }
}
